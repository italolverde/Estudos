numero = ''
primeironum = false

function clicknum(num) {
    numero += String(num)
    console.log(numero)
}
function op(operador) {
    if (primeironum) {
        
    }
    switch(operador) {
        case 1 :

        
    }
}